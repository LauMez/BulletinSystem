
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Editar preceptor</title>

    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.2/css/all.min.css" integrity="sha512-SnH5WK+bZxgPHs44uWIX+LLJAJ9/2PkPKZ5QiAj6Ta86w+fsb2TkcmfRyVX3pBnMFcV7oQPJkl9QevSCWr3W6A==" crossorigin="anonymous" referrerpolicy="no-referrer" />
    <link rel="stylesheet" href="/directive/css/editStudent.css">
</head>
<body>
    <div class="exit btn_back" id="btn_back">
        <i class="fas fa-caret-square-left"></i>
    </div>
    <div class="container">
        <h1>Modificar</h1>
        <div class="formulario">
            <label for="">Nombre:</label>
            <input type="text" placeholder="<%= data.preceptor.personalInformation.first_name %> <%= data.preceptor.personalInformation.second_name || '' %> <%= data.preceptor.personalInformation.last_name1 %> <%= data.preceptor.personalInformation.last_name2 || '' %>" readonly>
            <label for="">Documento:</label>
            <input type="text" placeholder="<%= data.preceptor.personalInformation.DNI %>" readonly>
            <label for="">Curso:</label>
            <select id="course" name="course">
                <% data.courses.forEach(course => { %>
                    <option><%= course.year %>°<%= course.division %></option>
                <% }) %>
            </select>
        </div>
        <input type="submit">

    </div>  

    <script>
        document.addEventListener('DOMContentLoaded', function() {
            var backButton = document.getElementById('btn_back')

            backButton.addEventListener('click', function() {
                if(window.history.length > 1) window.history.back()
                else alert('No hay historial para volver')
            })
        })
    </script>
</body>
</html>