<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Probar API Rest</title>
  <style>
    article {
      border: 1px solid #ccc;
      padding: 1rem;
      margin-bottom: 1rem;
    }

    img {
      height: auto;
      width: 320px;
      max-width: 100%;
    }
  </style>
</head>
<body>
  <main></main>

  <% if(subjects.length > 1 ) { %>
    <% subjects.forEach(course => { %>
      <article>
        <h1><%=course.year%>°-<%=course.division%>°</h1>
        <p>Turno: <%=course.entry_time%></p>
        <p>Especialidad: <%=course.specialty%></p>
      </article>
    <% }) %>
  <%} else {%>
    <article>
      <h1><%=subjects.year%>-<%=subjects.division%></h1>
      <p>Turno: <%=subjects.entry_time%></p>
      <p>Especialidad: <%=subjects.specialty%></p>
    </article>
  <%}%>
</body>
</html>